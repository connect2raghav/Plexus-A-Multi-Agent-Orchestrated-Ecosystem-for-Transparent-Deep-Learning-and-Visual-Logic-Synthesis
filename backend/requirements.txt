# Plexus Orchestration – Python dependencies
# Install with: pip install -r requirements.txt

# ------- Web framework -------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # file uploads

# ------- Core data science -------
pandas>=2.0.0
numpy>=1.24.0

# ------- LLM client -------
openai>=1.0.0              # OpenAI Chat API (GPT-4o / GPT-3.5-turbo)
google-genai>=0.8.0         # Google Gemini API – new SDK (free tier available)

# ------- Environment & config -------
python-dotenv>=1.0.0       # Load .env files

# ------- Data validation -------
pydantic>=2.0.0            # Schema enforcement for agent inputs/outputs

# ------- ML -------
scikit-learn>=1.3.0        # StandardScaler, MinMaxScaler, LabelEncoder

# ------- Deep learning (for training backend) -------
# tensorflow>=2.13.0       # Uncomment to enable TF training
# torch>=2.0.0             # Uncomment to enable PyTorch training

# ------- Pillow (image dataset previews) -------
Pillow>=10.0.0
